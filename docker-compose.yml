version: '3'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - 3001:3000
    environment:
      - NODE_ENV=production
    # Optional: Volume for hot reloading in dev (uncomment if using dev target)
    # volumes:
    #   - .:/app
    #   - /app/node_modules
